#include "sort.h"
/**
 * shell_sort - sort an array in ascending order using the shell sort algo.
 * @array: the array to sort.
 * @size: array size.
 * Return: nothing.
 */
void shell_sort(int *array, size_t size)
{
}
